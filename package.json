{
  "name": "sw-master",
  "version": "1.0.0",
  "description": "AI-Ready Senior Software Engineering Agent",
  "scripts": {
    "test": "npm run test --prefix frontend && pytest backend/tests",
    "scan": "npx @aiready/cli scan",
    "visualise": "npx @aiready/cli visualise"
  },
  "devDependencies": {
    "@aiready/cli": "latest",
    "jest": "latest",
    "pytest": "latest"
  }
}
